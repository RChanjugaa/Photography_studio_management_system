// minimal mock functions used by calendar and other modules
export function calendarRange(startISO, endISO) {
  // Return an empty list by default; replace with a real data generator as needed.
  return [];
}

// optionally export other mocks to avoid future errors
export const listPackages = () => [];
export const listClients = () => [];
